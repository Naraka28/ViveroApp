@model ViveroApp.Dto.CrearPlantaDto
@{
    ViewData["Title"] = "Crear Nueva Planta";
}

<section class="my-[3rem]">
    <h2 class="text-4xl font-bold text-gray-800 mb-6">
        <i class="ph ph-plus-circle text-green-600"></i>
        Crear Nueva Planta
    </h2>

    <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6 lg:p-8">
        <form asp-action="CrearPlanta" method="post" id="formCrearPlanta" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-red-600 mb-4"></div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                    <div class="mb-4">
                        <label asp-for="Nombre" class="block text-sm font-medium text-gray-700 mb-1">Nombre Común</label>
                        <input asp-for="Nombre" class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500" />
                        <span asp-validation-for="Nombre" class="text-red-600 text-sm"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="NombreCientifico" class="block text-sm font-medium text-gray-700 mb-1">Nombre Científico</label>
                        <input asp-for="NombreCientifico" class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500" />
                        <span asp-validation-for="NombreCientifico" class="text-red-600 text-sm"></span>
                    </div>

                    <div class="mb-4">
                        <label class="block text-sm font-medium text-gray-700 mb-2">Imagen de la Planta (Opcional)</label>

                        <div class="border-2 border-dashed border-gray-300 rounded-lg p-6 text-center hover:border-green-500 transition-colors">
                            <div id="uploadPrompt" class="space-y-2">
                                <i class="ph ph-upload-simple text-4xl text-gray-400"></i>
                                <div>
                                    <label for="imagenInput" class="cursor-pointer text-green-600 hover:text-green-700 font-medium">
                                        Selecciona una imagen
                                    </label>
                                    <p class="text-sm text-gray-500">o arrastra y suelta aquí</p>
                                </div>
                                <p class="text-xs text-gray-400">PNG, JPG, WEBP hasta 5MB</p>
                            </div>

                            <div id="previewContainer" class="hidden space-y-3">
                                <img id="imagePreview" src="" alt="Preview" class="max-h-40 mx-auto rounded-lg" />
                                <p id="fileName" class="text-sm text-gray-600"></p>
                                <button type="button" onclick="removeImage()" class="text-red-600 hover:text-red-800 text-sm font-medium">
                                    <i class="ph ph-trash"></i> Eliminar
                                </button>
                            </div>
                        </div>

                        <input asp-for="ImagenFile"
                               type="file"
                               accept="image/*"
                               id="imagenInput"
                               onchange="previewImage(event)"
                               class="hidden" />
                        <span asp-validation-for="ImagenFile" class="text-red-600 text-sm"></span>
                    </div>
                    <div class="mb-4">
                        <label class="block text-sm font-medium text-gray-700 mb-2">Categorías</label>
                        <div class="relative">
                            <select id="categoriaSelect" class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500">
                                <option value="">Seleccionar categoría...</option>
                                @foreach (var cat in ViewBag.Categorias)
                                {
                                    <option value="@cat.Id" data-nombre="@cat.Nombre">
                                       @cat.Nombre
                                    </option>
                                }
                            </select>
                        </div>

                        <div id="categoriasSeleccionadas" class="flex flex-wrap gap-2 mt-3 min-h-[2rem]">
                        </div>

                        <div id="categoriasHiddenInputs"></div>
                    </div>

                    <div class="mb-4">
                        <label asp-for="Descripcion" class="block text-sm font-medium text-gray-700 mb-1">Descripción</label>
                        <textarea asp-for="Descripcion" rows="4" class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500"></textarea>
                        <span asp-validation-for="Descripcion" class="text-red-600 text-sm"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="CuidadosEspeciales" class="block text-sm font-medium text-gray-700 mb-1">Cuidados Especiales</label>
                        <textarea asp-for="CuidadosEspeciales" rows="3" class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500"></textarea>
                        <span asp-validation-for="CuidadosEspeciales" class="text-red-600 text-sm"></span>
                    </div>
                </div>

                <div>
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                        <div class="mb-4">
                            <label asp-for="RiegoId" class="block text-sm font-medium text-gray-700 mb-1">Tipo de Riego</label>
                            <select asp-for="RiegoId" asp-items="@(new SelectList(ViewBag.Riegos, "Id", "Nombre"))" class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500">
                                <option value="">Seleccionar Riego</option>
                            </select>
                            <span asp-validation-for="RiegoId" class="text-red-600 text-sm"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="LuzId" class="block text-sm font-medium text-gray-700 mb-1">Tipo de Luz</label>
                            <select asp-for="LuzId" asp-items="@(new SelectList(ViewBag.Luces, "Id", "Tipo"))" class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500">
                                <option value="">Seleccionar Luz</option>
                            </select>
                            <span asp-validation-for="LuzId" class="text-red-600 text-sm"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="SustratoId" class="block text-sm font-medium text-gray-700 mb-1">Tipo de Sustrato</label>
                            <select asp-for="SustratoId" asp-items="@(new SelectList(ViewBag.Sustratos, "Id", "Tipo"))" class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500">
                                <option value="">Seleccionar Sustrato</option>
                            </select>
                            <span asp-validation-for="SustratoId" class="text-red-600 text-sm"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Dificultad" class="block text-sm font-medium text-gray-700 mb-1">Dificultad</label>
                            <select asp-for="Dificultad" class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500">
                                <option value="">Seleccionar Dificultad</option>
                                <option value="baja">Baja</option>
                                <option value="media">Media</option>
                                <option value="alta">Alta</option>
                            </select>
                            <span asp-validation-for="Dificultad" class="text-red-600 text-sm"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="AlturaMaxCm" class="block text-sm font-medium text-gray-700 mb-1">Altura Máxima (cm)</label>
                            <input asp-for="AlturaMaxCm" type="number" class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500" />
                            <span asp-validation-for="AlturaMaxCm" class="text-red-600 text-sm"></span>
                        </div>

                        <div class="mb-4 flex items-center justify-center">
                            <div class="flex items-center h-full">
                                <input asp-for="Toxica" type="checkbox" class="h-5 w-5 text-green-600 border-gray-300 rounded focus:ring-green-500" />
                                <label asp-for="Toxica" class="ml-2 block text-sm font-medium text-gray-900">¿Es tóxica?</label>
                            </div>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label asp-for="Notas" class="block text-sm font-medium text-gray-700 mb-1">Notas Adicionales</label>
                        <textarea asp-for="Notas" rows="3" class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500"></textarea>
                        <span asp-validation-for="Notas" class="text-red-600 text-sm"></span>
                    </div>

                    <div class="bg-green-50 border border-green-200 rounded-lg p-4 mt-4">
                        <div class="flex items-start gap-3">
                            <i class="ph ph-info text-green-600 text-xl mt-0.5"></i>
                            <div class="text-sm text-green-800">
                                <p class="font-medium mb-1">Consejos:</p>
                                <ul class="list-disc list-inside space-y-1 text-xs">
                                    <li>La imagen es decorativa (opcional)</li>
                                    <li>Usa imágenes cuadradas para mejor visualización</li>
                                </ul>
                            </div>
                        </div>
                    </div>

                </div>
            </div>

            <div class="mt-8 flex justify-end gap-4">
                <a asp-action="Plantas" class="px-6 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50">
                    Cancelar
                </a>
                <button type="submit" class="bg-[#1E6241] hover:bg-[#195136] text-white px-6 py-2 rounded-lg inline-flex items-center gap-2">
                    <i class="ph ph-plus-circle"></i>
                    Crear Planta
                </button>
            </div>
        </form>
    </div>
</section>

@section Scripts {
    <script>
        const categoriasSeleccionadas = new Set();

        document.getElementById('categoriaSelect').addEventListener('change', function() {
            const selectedOption = this.options[this.selectedIndex];
            const categoriaId = selectedOption.value;
            const categoriaNombre = selectedOption.dataset.nombre;

            if (categoriaId && !categoriasSeleccionadas.has(categoriaId)) {
                categoriasSeleccionadas.add(categoriaId);

                const badge = document.createElement('div');
                badge.className = 'inline-flex items-center gap-1.5 px-3 py-1.5 bg-green-100 text-green-800 rounded-full text-sm font-medium';
                badge.dataset.id = categoriaId;
                badge.innerHTML = `
                    <span>${categoriaNombre}</span>
                    <button type="button" class="ml-1 hover:text-green-900" onclick="eliminarCategoria('${categoriaId}')">
                        <i class="ph ph-x"></i>
                    </button>
                `;

                document.getElementById('categoriasSeleccionadas').appendChild(badge);

                const hiddenInput = document.createElement('input');
                hiddenInput.type = 'hidden';
                hiddenInput.name = 'CategoriasIds';
                hiddenInput.value = categoriaId;
                hiddenInput.dataset.id = categoriaId;
                document.getElementById('categoriasHiddenInputs').appendChild(hiddenInput);

                selectedOption.disabled = true;
            }

            this.value = '';
        });

        function eliminarCategoria(categoriaId) {
            categoriasSeleccionadas.delete(categoriaId);

            const badge = document.querySelector(`[data-id="${categoriaId}"]`);
            if (badge) badge.remove();

            const hiddenInput = document.querySelector(`input[data-id="${categoriaId}"]`);
            if (hiddenInput) hiddenInput.remove();

            const option = document.querySelector(`#categoriaSelect option[value="${categoriaId}"]`);
            if (option) option.disabled = false;
        }

        function previewImage(event) {
            const file = event.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    document.getElementById('imagePreview').src = e.target.result;
                    document.getElementById('fileName').textContent = file.name;
                    document.getElementById('previewContainer').classList.remove('hidden');
                    document.getElementById('uploadPrompt').classList.add('hidden');
                }
                reader.readAsDataURL(file);
            }
        }

        function removeImage() {
            document.getElementById('imagenInput').value = '';
            document.getElementById('imagePreview').src = '';
            document.getElementById('fileName').textContent = '';
            document.getElementById('previewContainer').classList.add('hidden');
            document.getElementById('uploadPrompt').classList.remove('hidden');
        }
    </script>
}